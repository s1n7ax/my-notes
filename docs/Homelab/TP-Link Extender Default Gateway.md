# TP-Link Extender Default Gateway

The default gateway IP for TP-Link range extenders is:

```
192.168.0.254
```

Access the admin web interface by navigating to this address in a browser while connected to the extender's network.
